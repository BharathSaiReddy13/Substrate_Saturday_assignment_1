//1q
//a : [t, n] is creating an array of size n and type t and .len() returns suze of array or no of elements in array
fn main() {
    // Fill the blank with proper array type
    let arr: [i8;5] = [1, 2, 3, 4, 5];

    // Modify the code below to make it work
    assert!(arr.len() == 5);

    println!("Success!");
}
